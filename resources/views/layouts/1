<!DOCTYPE html>
<html lang="id">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>RENT THE TOOLS!!</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background: #f5f7fb;
            color: #333;
            min-height: 100vh;
        }

        /* Navbar Styling */
        .navbar {
            background: #ffffff !important;
        }

        .navbar-brand {
            color: #222 !important;
            font-weight: 700;
            letter-spacing: 0.05em;
        }

        .nav-link {
            color: #555 !important;
            font-weight: 500;
            transition: 0.3s;
        }

        .nav-link:hover,
        .nav-link.active {
            color: #0d6efd !important;
        }

        /* Card Styling */
        .content-wrapper {
            margin-top: 2rem;
            margin-bottom: 2rem;
        }

        .app-card {
            background: #fff;
            border-radius: 1rem;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.05);
            border: 1px solid rgba(0, 0, 0, 0.05);
        }

        .dropdown-menu {
            border: none;
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1);
            border-radius: 0.8rem;
        }
    </style>
</head>

<body>
    <nav class="navbar navbar-expand-lg shadow-sm sticky-top">
        <div class="container">
            <a class="navbar-brand" href="{{ route('welcome') }}">RENT THE TOOLS!!</a>

            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNavbar">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="mainNavbar">
                <ul class="navbar-nav ms-auto align-items-center">

                    <!-- HOME -->
                    <li class="nav-item">
                        <a class="nav-link" href="{{ route('welcome') }}">Home</a>
                    </li>

                    @auth
                    @if(auth()->user()->role == 'admin')
                    <li class="nav-item">
                        <a class="nav-link" href="{{ route('admin.dashboard') }}">Dashboard</a>
                    </li>

                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" data-bs-toggle="dropdown">Kelola</a>
                        <ul class="dropdown-menu">
                            <li><a class="dropdown-item" href="{{ route('admin.categories.index') }}">Kategori</a></li>
                            <li><a class="dropdown-item" href="{{ route('admin.tools.index') }}">Alat</a></li>
                            <li><a class="dropdown-item" href="{{ route('admin.users.index') }}">User</a></li>
                            <li>
                                <hr class="dropdown-divider">
                            </li>
                            <li><a class="dropdown-item" href="{{ route('admin.loans.index') }}">Peminjaman</a></li>
                            <li><a class="dropdown-item" href="{{ route('admin.returns.index') }}">Pengembalian</a></li>
                            <li><a class="dropdown-item" href="{{ route('admin.logs') }}">Log Aktivitas</a></li>
                        </ul>
                    </li>

                    @elseif(auth()->user()->role == 'petugas')
                    <li class="nav-item">
                        <a class="nav-link" href="{{ route('petugas.dashboard') }}">Validasi</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ route('petugas.laporan') }}">Laporan</a>
                    </li>

                    @else
                    <li class="nav-item">
                        <a class="nav-link" href="{{ route('peminjam.dashboard') }}">Daftar Alat</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ route('peminjam.riwayat') }}">Riwayat</a>
                    </li>
                    @endif

                    <!-- USER -->
                    <li class="nav-item dropdown ms-3">
                        <a class="nav-link dropdown-toggle fw-bold text-dark" href="#" data-bs-toggle="dropdown">
                            {{ auth()->user()->name }}
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end">
                            <li class="dropdown-item-text">
                                <small class="text-muted d-block">Login sebagai:</small>
                                <span class="badge bg-primary">{{ strtoupper(auth()->user()->role) }}</span>
                            </li>
                            <li>
                                <hr class="dropdown-divider">
                            </li>
                            <li>
                                <form action="{{ route('logout') }}" method="POST">
                                    @csrf
                                    <button class="dropdown-item text-danger">Logout</button>
                                </form>
                            </li>
                        </ul>
                    </li>

                    @else
                    <li class="nav-item ms-3">
                        <a href="{{ route('login') }}" class="btn btn-outline-primary btn-sm px-4">Login</a>
                    </li>
                    <li class="nav-item ms-2">
                        <a href="{{ route('register') }}" class="btn btn-primary btn-sm px-4">Daftar</a>
                    </li>
                    @endauth

                </ul>
            </div>
        </div>
    </nav>

    <main class="container content-wrapper">
        <div class="app-card p-4">
            @if(session('success')) <div class="alert alert-success">{{ session('success') }}</div> @endif
            @if(session('error')) <div class="alert alert-danger">{{ session('error') }}</div> @endif
            @yield('content')
        </div>
    </main>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
